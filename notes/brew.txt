brew notes
-------

brew update
brew upgrade

brew cleanup --force
brew prune

# Brew notes
# cd $(brew --repository)   where things are installed
# brew --cache     where stuff gets downloaded
# brewwork    my own command for where formulas are stored
